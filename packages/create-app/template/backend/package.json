{
  "name": "backend",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --watch --no-clear-screen src/server.ts",
    "build": "bun build src/server.ts --compile --minify --sourcemap --outfile dist/server",
    "db:generate": "bun --bun run drizzle-kit generate",
    "db:migrate": "bun --bun run drizzle-kit migrate",
    "db:studio": "bun --bun run drizzle-kit studio"
  },
  "dependencies": {
    "@orpc/server": "^1.5.2",
    "drizzle-orm": "^0.44.2",
    "pino": "^9.7.0",
    "valibot": "^1.1.0"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.3.3",
    "@types/bun": "^1.2.17",
    "drizzle-kit": "^0.31.1",
    "pino-pretty": "^13.0.0"
  }
}
