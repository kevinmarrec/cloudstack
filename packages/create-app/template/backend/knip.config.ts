import type { KnipConfig } from 'knip'

const config: KnipConfig = {
  include: [
    'classMembers',
    'duplicates',
    'enumMembers',
    'exports',
    'files',
    'nsExports',
    'nsTypes',
    'types',
  ],
}

export default config
