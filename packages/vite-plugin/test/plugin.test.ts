import { describe, expect, it } from 'vitest'

import CloudstackVitePlugin from '../src'

describe('plugin', () => {
  it('should work', () => {
    expect(CloudstackVitePlugin).toBeDefined()
  })
})
