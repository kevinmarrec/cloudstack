{
  "name": "playground",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "vite --open",
    "build": "vite-ssg build",
    "lint": "eslint . && stylelint --aei '**/*.{css,scss,vue}'",
    "serve": "vite preview"
  },
  "dependencies": {
    "@kevinmarrec/cloudstack-frontend": "workspace:*"
  },
  "devDependencies": {
    "@kevinmarrec/cloudstack-eslint-config": "workspace:*",
    "@kevinmarrec/cloudstack-stylelint-config": "workspace:*",
    "eslint": "^9.6.0",
    "stylelint": "^16.6.1"
  }
}
